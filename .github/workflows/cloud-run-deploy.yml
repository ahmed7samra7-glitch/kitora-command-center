name: KCC Cloud Run Deploy

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT_ID: ais-europe-west2-116319078cfc4
  GCP_REGION: europe-west2
  CLOUD_RUN_SERVICE: kitora-command-center

jobs:
  disabled:
    name: GCP deployment intentionally disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "GCP deployment disabled by KCC $0/no-card policy"
